
/* let secret = ('Ybrobrubr brabrrbrebr brtbrhbrebr brbbrebrsbrtbr brQbrAbr brebrvbrebrrbr');

function decoder(secret) {
    if (secret !== null) {
        let theSecret = secret.split(/Br|br|bR/).join('');
        return theSecret
    } else {
        return ('not valid input')
    }
}
console.log(decoder(secret)); */



/* function myFirstKata(a, b) {
    let result;
    if (isNaN(a) || isNaN(b) || a == 0 || b == 0 || typeof (a) !== 'number' || typeof (b) !== 'number') {
        result = false;
    }
    else {
        result = a % b + b % a;
    }
    return (result)
}

console.log(myFirstKata(true, 1)); */


